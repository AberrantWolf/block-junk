[package]
name = "texture-studio"
version.workspace = true
edition.workspace = true

[dependencies]
bevy = { version = "0.18", features = ["mouse", "keyboard"] }
bevy_egui = "0.39"
block-junk-textures = { path = "../block-junk-textures", features = ["render", "egui-fonts"] }
# "Export PNG" in the tiling preview.
png = "0.18"
